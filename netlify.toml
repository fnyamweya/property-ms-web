# Proxy API first (same-origin -> no CORS)
[[redirects]]
  from = "/api/*"
  to = "https://073gt8mm-8080.uks1.devtunnels.ms/api/:splat"
  status = 200
  force = true

# SPA fallback last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
